# include (stdio.h)
 
int main (void) 

{  
    int V0, V，chufa;

    printf ("Enter V0,V:\n"); 
    scanf("%d%d,&V0,&V");

    if ( (1+0.1)V0<V<=(1+0.5)V0 ){
    chufa = 200;
    }
    else if ( (1+0.5)V0<V ){
    chufa = diaoxiaojiazhao;
    }
    else {
        chufa = wu;
    }
    printf ("%d",chufa);
  return 0;
}